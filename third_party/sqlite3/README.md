# sqlite3 amalgamation

本目录包含构建 PassVault 所需的 SQLite 官方 amalgamation 源码：

- `sqlite3.c`
- `sqlite3.h`

## 下载

更新版本时，从 https://www.sqlite.org/download.html 获取
`sqlite-amalgamation-XXXXXXX.zip`（推荐版本 >= 3.45.0），解压后替换上述文件。

原始 ZIP 下载包和命令行 shell 源码不提交到 Git。
